<?php

// current dates
	
	// Year	
	if($_POST['date']['year']){
		$currentYear = $_POST['date']['year']; 
	} 
	else if($_GET['year']){
		$currentYear = $_GET['year'];
	} 
	else{
		$currentYear = date('Y');
	}
	
	// Month
	if($_POST['date']['month']){
		$currentMonth = $_POST['date']['month']; 
	} 
	else if($_GET['month']){
		$currentMonth = $_GET['month'];
	} 
	else{
		$currentMonth = date('m');
	}
	
	// Day
	if($_POST['date']['day']){
		$currentDay = $_POST['date']['day']; 
	} 
	else if($_GET['day']){
		$currentDay = $_GET['day'];
	} 
	else{
		$currentDay = date('d');
	}
	
	// Time
	
	// starts
	// hours
	if($_POST['starts']['hour']){
		$currentStartHour = $_POST['starts']['hour']; 
	}  
	else{
		$currentStartHour = '09';
	}
	// minutes
	if($_POST['starts']['minute']){
		$currentStartMinute = $_POST['starts']['minute']; 
	}  
	else{
		$currentStartMinute = '00';
	}
	
	//ends
	// hours
	if($_POST['ends']['hour']){
		$currentEndHour = $_POST['ends']['hour']; 
	}  
	else{
		$currentEndHour = '17';
	}
	// minutes
	if($_POST['ends']['minute']){
		$currentEndMinute = $_POST['ends']['minute']; 
	}  
	else{
		$currentEndMinute = '00';
	}

?>